// assign operator used for assigning value to variable

let x = 10;

// addition operator used for adding two numbers

let a = 23;

let b = 10;

let c = a + b;


// multiplication operator which used to make product of two or more numbers

let d = a * b;


// dividing operator which used to devide two numbers

let e = a / b;

//remainder(modulus) operator

let f = a % b;

// incrementing and decrementing operotor which used to add 1 or subtract 1 on the value

x++;

a--;

console.log(a);

// comparison operator

let g = 12;

console.log(g === "12");

// tenary operator

if (x == 12 && a == 23) {
    console.log("this some thing");
}

if (a == b || b == a) {
    console.log("some things");
    
}
